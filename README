testing out redmine github sync
and now to tryout the github post-receive url
will the simple github_hook request deal with case-sensitivity?
looks like the answer is no. well, just pass in the redmine project name via the project_id param